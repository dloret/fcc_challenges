import React from 'react';

const Keyboard = () => (
  <section id="keyboard">
    <div className="keys operands" />
    <div className="keys operators" />
    <div className="keys funcs" />
  </section>
);

export default Keyboard;
